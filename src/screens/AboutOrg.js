/*  IT'S ALL USELESS.  DELETE FILE

import React, { useState, useEffect } from "react";
import {
  View,
  StyleSheet,
} from "react-native";
import {
  Modal,
  Portal,
  Text,
  IconButton
} from "react-native-paper";

const AboutOrg = ({ showAboutOrgModal }) => {
  //const [showAboutOrgModal, setShowAboutOrgModal] = useState(false);


  return (
    <Portal>
      <Modal visible={showAboutOrgModal} style={{ flex: 1 }}>
        <Text>
          Hello, I'm David, your writer.
        </Text>
        <IconButton
          icon="cancel"
          onPress={() => setShowAboutOrgModal(false)}
        />


      </Modal>
    </Portal>
  )
}

export default AboutOrg;*/